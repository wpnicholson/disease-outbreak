# backend/pytest.ini
[pytest]
minversion = 7.0
addopts = --strict-markers --tb=short -ra
testpaths =
    tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
pythonpath = .
# Optional: Show warnings summary at the end
filterwarnings =
    ignore::DeprecationWarning
